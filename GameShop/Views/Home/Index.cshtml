@model IEnumerable<Domain.Models.Product>
@{
    ViewData["Title"] = "Home Page";
}



    @foreach(var item in Model)
    }
       @Html.ActionLink(item.Name,"Index","Product",new {Category=item.Name})
    }

